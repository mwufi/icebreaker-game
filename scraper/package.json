{
  "name": "profiles-bun-drizzle",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bun run src/migrate.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.33.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.24.0"
  }
}
